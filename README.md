# m3u-maker

make .m3u8 files so you have quick playlists!
pass in a directory to recursively walk through and make a .m3u8 playlist for each directory:
  - that doesn't already have a .m3u8 file
  - that does have music files
  - that does not contain a nested subdirectory that contained music files

